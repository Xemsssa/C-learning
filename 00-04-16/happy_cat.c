int main(int argc, char const *argv[])
{
	/* code */
	char* str = "happy cat";
	int counter = 0;

	for (char* ptr = str; *ptr != '\0'; ++ptr)
	{
		/* code */
		counter++;
	}

	printf("%d\n", couter);
	return 0;
}